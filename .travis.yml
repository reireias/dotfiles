sudo: required
language: c
os:
  - linux
install:
  - curl -L raw.github.com/reireias/dotfiles/master/install.sh | bash
  - cd ~/.dotfiles
  - sudo ./initialize.sh
script:
  - echo "TODO: shellcheck"

