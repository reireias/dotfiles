sudo: required
language: sh
os:
  - linux
addons:
  apt:
    packages:
      - shellcheck
install:
  - curl -L raw.github.com/reireias/dotfiles/master/install.sh | bash
  - sudo ~/.dotfiles/initialize.sh
script:
  - test/test.sh

